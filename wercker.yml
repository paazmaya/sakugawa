box: wercker/nodejs
build:
  steps:
    - npm-install
    - script:
        name: nodejs and npm version information
        code: npm --versions
    - script:
        name: ESLint everything!
        code: npm run lint
    - npm-test
